import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "p16-sign.tiktokcdn.com", "p19-sign.tiktokcdn.com"],
  },
};

export default nextConfig;
